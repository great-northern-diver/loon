#' @return if the argument \code{by} is not set, a \code{loon} widget will be returned;
#' else an \code{l_facet} object (a list) will be returned and each element is
#' a \code{loon} widget displaying a subset of interest.
